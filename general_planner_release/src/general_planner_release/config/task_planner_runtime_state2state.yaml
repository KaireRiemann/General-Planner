# Runtime state2state profile: click-demo corridor + fast-LBFGS route with
# conservative 5 m/s flight limits. GlobalMapRuntime owns the world map from
# config/exploration_rog_map.yaml; the mirrored ROG section below therefore
# uses the same Villa exploration bounds and has no ROS callbacks.
fsm:
  task_mode: "state2state"
  task_planner_en: true
  task_mode_topic: "/planning/navigation_task_mode"
  click_goal_en: true
  click_goal_topic: "/planning/click_goal"
  click_goal_3d_en: true
  # In composed planner_runtime, PlannerSupervisor owns the external
  # /goal_3d ingress and forwards accepted goals here.  Do not subscribe to
  # /goal_3d directly, or a 3D goal bypasses task lifecycle/command handover.
  click_goal_3d_topic: "/planning/click_goal_3d"
  click_height: 1.5
  click_yaw_en: true
  # PlannerSupervisor can switch this composed adapter to `tracking` at
  # runtime. Keep Odom as the fallback input, but prefer a fresh prediction
  # Path when an upstream predictor owns the full horizon.  This matches the
  # complete tracking profile from 551caef while still accepting Odom-only
  # target sources.
  # Unity/external target estimator publishes the tracked object's state
  # directly.  When no prediction Path is available, FsmRos1 derives the
  # configured 4 s kinematic prediction from this Odometry stream.
  tracking_target_odom_topic: "/target_ekf_node/target_odom"
  tracking_target_prediction_topic: "/tracking/target_prediction"
  tracking_use_target_prediction_path: true
  perching_surface_odom_topic: "/perching/surface_odom"
  tracking_prediction_horizon: 4.0
  tracking_prediction_dt: 0.25
  tracking_prediction_use_kinodynamic: false
  tracking_prediction_accel: 3.0
  tracking_prediction_vmax: 4.0
  tracking_prediction_rho_accel: 1.0
  tracking_prediction_max_time: 0.03
  tracking_static_position_epsilon: 0.05
  tracking_static_velocity_epsilon: 0.05
  tracking_static_yaw_epsilon: 0.05
  tracking_static_replan_remaining_time: 0.8
  tracking_static_task_position_epsilon: 0.12
  tracking_static_task_velocity_epsilon: 0.10
  tracking_static_prediction_filter_velocity_epsilon: 0.08
  tracking_static_safety_check_horizon: 1.5
  tracking_static_safety_check_dt: 0.12
  tracking_static_replan_log_period: 1.0
  tracking_plan_from_rest_max_failures: 4
  tracking_plan_from_rest_failure_backoff: 0.5
  tracking_plan_from_rest_limited_backoff: 2.0
  tracking_static_finish_on_plan_failure: false
  task_timeout: 1.0
  replan_rate: 15.0
  # One bound covers both rolling replan and plan-from-rest. The latter has
  # no command stream until a trajectory is committed, so it reports through
  # its own watchdog topic while supervisor keeps a verified HOLD.
  state2state_replan_watchdog_timeout: 2.0
  state2state_replan_watchdog_topic: "/planning/navigation/replan_watchdog"
  state2state_plan_watchdog_topic: "/planning/navigation/plan_watchdog"
  cmd_topic: "/planning/pos_cmd"
  mpc_cmd_topic: "/planning_cmd/poly_traj"
  timer_en: true
  # click_demo leaves the enable flag unset (false by Config default).  Keep
  # the same policy in this comparison baseline; do not inherit runtime's
  # additional perception-triggered replans.
  perception_replan_check_en: false
  perception_replan_check_rate: 60.0
  perception_replan_check_horizon: 2.0
  perception_replan_check_dt: 0.06
  perception_replan_min_interval: 0.08
  perception_replan_consecutive_hits: 1
  perception_replan_unknown_as_occupied: false
  perception_replan_emergency_horizon: 0.45

general_planner:
  backup_traj_en: true
  esdf_traj_en: false
  esdf_safe_distance: 0.3
  detailed_log_en: false
  visualization_en: true
  use_fov_cut: false
  print_log: false
  visual_process: false
  frontend_in_known_free: false
  goal_yaw_en: false
  goal_vel_en: false
  corridor_bound_dis: 2.0
  corridor_line_max_length: 2.0
  safe_corridor_line_max_length: 8.0
  iris_iter_num: 2
  ellipsoid_optimizer: classic
  ellipsoid_optimizer_fallback: false
  obs_skip_num: 2
  replan_forward_dt: 0.1
  planning_horizon: 8.0
  sensing_horizon: -1
  receding_dis: 5.0
  robot_r: 0.2
  frontend_astar_time_out: 0.35
  over_wall_search_en: true
  over_wall_max_climb: 3.0
  over_wall_height_step: 0.3
  over_wall_min_blocked_span: 0.8
  over_wall_min_progress_gain: 0.5
  over_wall_forward_ratio: 0.9
  dynamic_obstacle_layer:
    enable: true
    cloud_topic: "/cloud_registered"
    cloud_queue_size: 3
    odom_timeout: 0.2
    ttl: 0.35
    voxel_size: 0.20
    inflation_radius: 0.40
    line_check_step: 0.05
    max_points_per_frame: 30000
    local_half_size: [ 12, 12, 3 ]
  state2state:
    direct_line_frontend_enable: true
    topology:
      enable: true
      # This profile permits the M3 global-topology option. It does not turn
      # the option on: local-only remains the default until the user publishes
      # true on /planner/navigation/use_global_topology.
      query_capability_enable: true
      # With global topology selected, a distant goal must remain on a
      # topology route.  A failed rejoin is a safe planning failure, never a
      # fallback to a direct/free-space route.
      strict_route_enable: true
      selection_topic: /planner/navigation/use_global_topology
      # Global bubble A* supplies a route; the local planner executes only the
      # collision-checked prefix inside the rolling ROG window.
      # Sparse persistent 3D skeleton over sensor-observed global free space.
      construction_mode: persistent_bubble_skeleton
      planar_mode: false
      navigation_altitude: 1.5
      unknown_as_free: false
      min_query_distance: 8.0
      local_prefix_length: 8.0
      local_boundary_margin: 0.8
      route_rejoin_max_candidates: 3
      route_deviation_requery_distance: 1.0
      route_query_min_interval: 0.5
      # Dirty regions nearest to current odom are consumed asynchronously.
      update_budget: 8
      update_period: 0.05
      publish_period: 0.20
      region_size: 4.0
      sample_spacing: 0.8
      # Nodes still require KNOWN_FREE and inflated-safe edges; 0.20 m lets
      # first-pass LiDAR ray evidence seed the graph at robot-radius clearance.
      min_clearance: 0.20
      max_clearance: 2.5
      # Committed nodes never move; global spacing prevents clusters at dirty
      # region boundaries while later observations fill uncovered gaps.
      candidate_separation: 1.5
      stable_match_distance: 1.0
      connection_radius: 6.0
      edge_sample_spacing: 0.20
      dirty_padding: 2.5
      bubble_overlap_margin: 0.10
      max_nodes_per_region: 12
      max_bubbles_per_region: 192
      max_neighbors: 8
    altitude_guard_enable: true
    altitude_band: 0.25
    altitude_escape_enable: true
    altitude_escape_band: 3.5
    over_goal_guard_enable: true
    over_goal_tolerance: 0.25
    near_goal_radius: 2.0
    accept_exp_without_backup_near_goal: true
  unknown_goal_reveal_en: true
  yaw_dot_max: 3.0
  # Yaw mode: 1 heading to velocity, 2 heading to goal
  yaw_mode: 1
  mpc_horizon: 15
  # The composed runtime initializes one FsmRos1 with this configuration and
  # changes its task mode in place.  Keep the tracking-specific profile here
  # rather than relying on Config defaults: without it, MODE_TRACKING has the
  # correct state machine but loses the complete viewpoint, visibility,
  # recovery, and keep-old policy tuned in 551caef's tracking.yaml.
  tracking:
    distance: 2.2
    distance_tolerance: 0.8
    height_offset: 0.7
    height_tolerance: 0.6
    safe_distance: 0.35
    hard_safe_distance: 0.22
    narrow_passage_enable: true
    narrow_passage_clearance_threshold: 0.38
    narrow_passage_soft_safe_distance_scale: 0.65
    visibility_safe_distance: 0.25
    visibility_cone_ratio: 0.12
    visibility_angle_clearance: 0.0872664626
    adaptive_occlusion_enable: true
    adaptive_occlusion_activation_distance: 0.25
    adaptive_occlusion_max_weight_scale: 12.0
    adaptive_occlusion_recovery_oe_scale: 8.0
    adaptive_occlusion_od_far_weight_scale: 4.0
    adaptive_occlusion_distance_upper_scale: 0.65
    adaptive_occlusion_min_horizontal_upper: 0.85
    adaptive_occlusion_postcheck_enable: true
    reacquire_distance: 6.0
    min_commit_duration: 0.8
    low_speed_velocity_threshold: 0.25
    angular_hysteresis: 0.35
    anti_rollback_enable: true
    anti_rollback_horizon: 1.0
    anti_rollback_dt: 0.25
    anti_rollback_margin: 0.35
    keep_old_horizon: 1.0
    keep_old_safety_dt: 0.05
    keep_old_short_safety_grace_enable: true
    keep_old_short_safety_grace_horizon: 0.25
    keep_old_min_remaining: 0.45
    keep_old_min_speed: 0.15
    keep_old_min_displacement: 0.04
    keep_old_min_progress_ratio: 0.10
    keep_old_min_progress_3d_ratio: 0.08
    keep_old_max_tracking_error_scale: 1.25
    max_consecutive_keep_old: 2
    no_motion_guard_enable: true
    no_motion_check_horizon: 0.35
    no_motion_min_displacement: 0.04
    no_motion_min_displacement_z: 0.04
    no_motion_target_speed_threshold: 0.25
    motion_3d_enable: true
    vertical_motion_threshold: 0.12
    detour_grace_enable: true
    detour_grace_horizon: 1.2
    detour_max_tracking_error_scale: 2.5
    anti_rollback_eval_after_prefix: true
    recovery_enable: true
    recovery_horizon: 1.5
    recovery_distance_tolerance_scale: 1.8
    recovery_height_tolerance_scale: 1.8
    recovery_time_scale: 1.4
    recovery_reduce_visible_region_weight: 0.3
    recovery_reduce_target_forward_weight: 0.5
    # These post-551 guards are deliberately disabled here, so the runtime
    # profile retains 551caef's recovery semantics.
    soft_recovery_enable: false
    soft_recovery_margin: 0.2
    soft_recovery_hold_duration: 0.8
    reacquire_recovery_enable: true
    reacquire_recovery_horizon: 0.8
    reacquire_transit_enable: true
    reacquire_transit_horizon_scale: 0.6
    reacquire_transit_max_horizon: 2.5
    reacquire_visible_region_weight_scale: 0.0
    reacquire_fov_relax_enable: true
    reacquire_fov_deferred_strict_enable: true
    reacquire_fov_entry_distance: 6.0
    reacquire_min_progress_distance: 0.5
    reacquire_min_progress_ratio: 0.10
    reacquire_fov_range_grace: 8.0
    reacquire_fov_angular_grace_deg: 8.0
    optimizer_commit_safety_precheck_enable: true
    retry_without_corridor_enable: true
    candidate_angle_step: 0.3926990817
    candidate_radius_num: 3
    visibility_samples: 5
    fallback_relax_enable: true
    fallback_distance_tolerance_scale: 1.6
    fallback_height_tolerance_scale: 1.5
    fallback_candidate_radius_extra: 2
    fallback_candidate_angle_step_scale: 0.5
    fallback_search_horizon_scale: 1.3
    weight_od_near: 35.0
    weight_od_far: 5.0
    weight_od_vertical: 8.0
    weight_oa: 5.0
    weight_oe: 1.0
    weight_relative_velocity: 1.0
    weight_tangent_velocity: 5.0
    weight_viewpoint_attractor: 25.0
    weight_visible_region: 3.0
    weight_fov: 80.0
    weight_target_forward: 60.0
    static_distance_tolerance_scale: 0.35
    static_height_tolerance_scale: 0.5
    static_tangent_weight_scale: 3.0
    static_tail_speed_epsilon: 0.08
    fov_horizontal_deg: 90.0
    fov_vertical_deg: 60.0
    fov_range: 4.0
    target_front_margin: 0.05
    fov_commit_check_enable: true
    fov_check_strict: false
    fov_check_dt: 0.03
    fov_range_grace_enable: true
    fov_range_grace: 0.9
    fov_keep_old_angular_grace_deg: 5.0
    fov_keep_old_violation_ratio_grace: 0.25
    fov_range_margin: 0.05
    fov_front_margin: 0.05
    fov_check_first_commit: true
    keep_old_requires_fov: true
    frontend_fov_feasibility_enable: true
    frontend_yaw_rate_feasibility_enable: true
    frontend_fov_range_margin: 0.05
    frontend_yaw_rate_margin: 0.10
    frontend_obstacle_recovery_enable: true
    frontend_grid_neighbor_mode: 26
    frontend_over_wall_enable: true
    frontend_over_wall_max_climb: 2.0
    frontend_side_pass_enable: true
    frontend_side_pass_width: 1.5
    frontend_reacquire_relax_yaw_rate: true
    joint_sample_dt: 0.05
    dense_joint_sample_enable: true
    unknown_as_occupied: false
    use_visible_region: true
    frontend_astar: true
    use_snap: false


traj_opt:
  switch:
    save_log_en: false
    # Keep per-optimization details in CSV; print only the final aggregate summary.
    print_optimizer_log: false

  boundary:
    max_vel: 5.0
    max_acc: 7.0
    max_jerk: 70.0
    max_omg: 3.5
    max_tilt: 1.05
    max_acc_thr: 22.0
    min_acc_thr: 6.0
    penna_margin: 0.05

  exp_traj:
    # 1 for xi 2 for pos
    pos_constraint_type: 1
    energy_cost_type: 4
    block_energy_cost: false
    opt_accuracy: 5.0e-6
    # Shared LBFGS fast path for the ordinary state2state ExpTrajOpt. It is
    # representation-independent and therefore applies to both dense and
    # fixed-depth convex-hull objectives.
    lbfgs_fast_en: true
    lbfgs_mem_size: 32
    lbfgs_step_bound_en: false
    lbfgs_time_ratio_min: 0.5
    lbfgs_time_ratio_max: 2.0
    lbfgs_fast_window: 3
    lbfgs_fast_min_iterations: 10
    lbfgs_fast_consecutive: 1
    lbfgs_fast_rel_cost: 1.0e-3
    lbfgs_fast_rel_step: 2.0e-2
    lbfgs_fast_rel_penalty: 5.0e-2
    # Keep the stable fast-stop rule based on objective/decision-step/penalty
    # changes. The phase-0 raw-gradient guard is scale-sensitive under the
    # large corridor weights and otherwise prevents nearly every fast stop.
    # Continuous-time feasibility is checked independently by the certificate.
    lbfgs_fast_phase0_guards_en: false
    lbfgs_fast_rel_time: 2.0e-2
    lbfgs_fast_rel_waypoint: 2.0e-2
    lbfgs_fast_scaled_grad: 5.0e-2
    lbfgs_fast_min_step: 1.0e-8
    lbfgs_fast_penalty_tol: 1.0e-2
    lbfgs_fast_small_step_limit: 3
    # Reuse the previous successful physical MINCO solution only when it is
    # compatible with the new corridor. The fresh guide seed and cached seed
    # are evaluated with the same real objective; the cached seed wins only
    # when it improves the guide cost by at least 25%.
    lbfgs_warm_start_en: false
    lbfgs_warm_start_max_endpoint_shift: 3.0
    lbfgs_warm_start_max_waypoint_shift: 2.0
    lbfgs_warm_start_duration_blend: 0.75
    lbfgs_warm_start_cost_ratio: 0.75
    lbfgs_warm_start_gradient_ratio: 1.0
    lbfgs_warm_start_penalty_ratio: 1.0
    # Preserve the timing of the already feasible guide trajectory. The old
    # implicit 0.8 scale manufactured dynamic violations before every solve.
    guide_initial_time_scale: 1.0
    scale_factor: 1.0
    integral_reso: 15
    # Representation selection. All convex-hull formulation parameters and its
    # independent penalty weights live in the dedicated profile below.
    # Set false to run dense using only the penna_* block in this file.
    convex_hull_en: true
    convex_hull_config: "traj_opt/convex_hull/click_real_highspeed.yaml"
    smooth_eps: 0.01
    # Dense-integral-only penalty set. It is ignored by the convex-hull
    # polynomial/control-point terms whenever the profile above is active.
    penna_t: 2.0e+5
    penna_pos: 5.0e+7
    penna_vel: 1.0e+7
    penna_acc: 1.0e+7
    # penna_jerk <= 0 disables both the penalty and continuous jerk certificate.
    penna_jerk: -1.0e+5
    penna_attract: 1.0e+3
    penna_guide_path: 0.0
    guide_path_tube_radius: 0.05
    guide_path_z_tube_radius: 0.05
    guide_path_huber_delta: 0.40
    guide_path_time_gradient_en: false
    penna_guide_z_tube: 1.0e+7
    guide_z_tube_radius: 0.00
    penna_omg: 5.0e+6
    penna_thr: 2.0e+6

  backup_traj:
    uniform_time_en: true
    pos_constraint_type: 1
    piece_num: 2
    energy_cost_type: 4
    block_energy_cost: false
    opt_accuracy: 1.0e-6
    scale_factor: 1.0
    integral_reso: 12
    smooth_eps: 0.01
    penna_t: 5.0e+4
    penna_ts: 1.0e+6
    penna_pos: 1.0e+8
    penna_vel: 1.0e+7
    penna_acc: 1.0e+7
    penna_jerk: -1.0e+7
    penna_guide_z_tube: 5.0e+6
    guide_z_tube_radius: 0.00
    penna_omg: 5.0e+6
    penna_thr: 2.0e+6

  esdf_traj:
    block_energy_cost: false
    opt_accuracy: 5.0e-6
    integral_reso: 15
    smooth_eps: 0.01
    penna_t: 12000.0
    penna_pos: 5.0e+6
    penna_vel: 5.0e+5
    penna_acc: 5.0e+5
    penna_jerk: 5.0e+5
    penna_omg: 5.0e+5
    penna_thr: 1.0e+5

  flatness:
    mass: 1.64
    dh: 0.35
    dv: 0.35
    cp: 0.001
    v_eps: 0.0001
    grav: 9.81


astar:
  map_voxel_num: [ 500, 500, 100 ]
  visual_process: false
  allow_diag: true
  # 0 DIAG; 1 MANHATTAN; 2 EUCLIDEAN
  heu_type: 2
  debug_visualization_en: false


rog_map:
  resolution: 0.15
  inflation_resolution: 0.15
  inflation_step: 3
  unk_inflation_en: false
  unk_inflation_step: 1
  map_size: [ 72.0,72.0,8.0 ]
  # If the map rolling is disable, the map origin [m] should be set.
  fix_map_origin: [ 0,0,1.5 ]
  frontier_extraction_en: false

  # Virtual ceil and ground
  virtual_ceil_height: 6.4
  virtual_ground_height: -0.1

  load_pcd_en: false

  map_sliding:
    enable: true
    # The minimum distance [m] to slide the map.
    threshold: 0.3

  esdf:
    enable: false
    resolution: 0.1
    # The range of esdf around the odom [m].
    local_update_box: [ 12.0,12.0,4.0 ]

  # If [enable = true], the ROG-Map will actively take ros topic as input.
  #  else user should call function [updateMap] to update the map.
  ros_callback:
    enable: false
    cloud_enable: false
    cloud_topic: "/cloud_registered"
    odom_topic: "/lidar_slam/odom"
    odom_timeout: 0.10


  visualization:
    enable: true
    # when enabled, use rqt_reconfigure to change the visualization parameters.
    use_dynamic_reconfigure: false
    # The rate of visualization [Hz], if set to 0, the visualization will not be triggered by frame.
    time_rate: 1.0
    # The frame rate of visualization [Hz], if set to 0, the visualization will not be triggered by frame.
    frame_rate: 0
    # visualization range along the odom [m].
    range: [ 30.0,30.0,5.0 ]
    frame_id: "world"
    pub_unknown_map_en: false

  # Intensity filter for input point cloud. If the point intensity is less than this value,
  # it will be ignored (to avoid noise like dusk).
  intensity_thresh: -10
  # The temporal downsample rate for input point cloud.
  #  e.g. if set to k, the point cloud will be downsampled to 1/k.
  point_filt_num: 1

  # Probabilistc update
  raycasting:
    # if disable, the map will only maintain occupied information, and all other grid
    # will be considered as unknown.
    # Persistent topology requires miss-ray integration; otherwise ROG contains
    # OCCUPIED and UNKNOWN only and cannot supply observed KNOWN_FREE evidence.
    enable: true
    batch_update_size: 1
    local_update_box: [ 70.0,70.0,6.0 ]
    # The range of raycasting [m].
    ray_range: [0.50, 60.0 ]

    p_min: 0.12
    p_miss: 0.49
    p_free: 0.499
    p_occ: 0.85
    p_hit: 0.9
    p_max: 0.97
    # The threshold of a non-occupied inf map grid to be considered as free. [0.0-1.0]
    # for counter map, if each cell contains N cells in prob map, then only when
    # N_unk > N * unk_thresh cells are unknown, the cell will be considered as unknown.
    # example: 1) if unk_thresh is set 0.0, then once at least one cell in prob map is unknown,
    #             the cell will be considered as unknown.
    #         2) if unk_thresh is set to 1.0, then only all cells in prob map are unknown,
    #             the cell will be considered as unknown.
    # in all, 1.0 is more aggressive while 0.0 is more conservative.
    unk_thresh: 1.0
